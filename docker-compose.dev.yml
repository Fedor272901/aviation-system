services:

  backend:
    volumes:
      - ./backend:/app
    environment:
      DEBUG: "true"
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload

  nginx:
    ports:
      - "8080:80"